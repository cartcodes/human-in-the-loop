# Webhook Approval System Configuration
# Copy this file to .env and update with your values

# API Key for webhook registration (REQUIRED - change this to a secure value in production!)
API_KEY=your-secure-api-key-here

# Server port (optional, defaults to 3000)
PORT=3000

# Public domain for ngrok (e.g., your-domain.ngrok.app)
# This is used for generating URLs in documentation and test webhooks
DOMAIN=your-domain.ngrok.app

# Optional: Add more configuration as needed
# NODE_ENV=production
# LOG_LEVEL=info